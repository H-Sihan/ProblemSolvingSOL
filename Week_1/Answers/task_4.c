#include <stdio.h>

int main() {
    int firstNumber = 8;
    int secondNumber = 7;
    int answer = firstNumber + secondNumber;
    
    printf("%d + %d = %d\n", firstNumber, secondNumber, answer);
}
