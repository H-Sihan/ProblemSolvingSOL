#include <stdio.h>

int main(){
    int firstNumber = 27;
    int lastNumber = 27;
    int answer = firstNumber + lastNumber;
    printf("%d + %d = %d", firstNumber,lastNumber,answer);
    return 0;
}